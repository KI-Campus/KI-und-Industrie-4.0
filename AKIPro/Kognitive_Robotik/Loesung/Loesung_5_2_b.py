solution = str('Welcher Anteil der Trainingsdaten für die Validierung verwendet wird, wird durch die Variable "validation split" in der Funktion model.fit angegeben. Der angegebene Wert von 0.2 bedeutet, dass 20 % der Trainingsdaten für die Validierung  verwendet werden.') 
print(solution)
